請幫我建立一個活動網站的前端頁面框架，先以「可操作、可預覽、之後方便替換內容」為目標，不需要先有完整真實資料。

## 目標
我要做一個活動網站，主要給活動參加者在手機上快速查看資訊，並依場次找到自己的活動資訊。
目前內容資料還沒完全備齊，所以請先產生「網站框架版」，將圖片、時間表、節目資料等區塊先用 placeholder 或假資料呈現，但整體版型、結構、互動方式要先完整。

## 網站結構
網站包含：
1. 首頁
2. 活動資訊頁（斗南場）
3. 活動資訊頁（竹北場）

## 首頁需求
首頁是 domain 進來的第一頁，畫面要簡潔，第一屏只做場次分流。

請顯示：
- 主標題：請選擇您參加的場次
- 副標題：選擇後即可查看活動時間、地點、演出資訊與導航方式

放兩個大按鈕：
- 斗南場
- 竹北場

按鈕點下去後，分別導向對應的活動資訊頁。

請以手機版體驗優先設計，按鈕要大、清楚、易點擊。
畫面風格簡潔、明亮、現代，不要過度花俏。

## 活動資訊頁需求
每個場次各有一頁，頁面內容採「一頁式設計」，不要再切很多子頁。
頁面上方需要有固定導覽列，點擊後可平滑捲動到對應區塊。

斗南場導覽列項目如下：
- 場地資訊
- 時間表
- 節目表
- 我迷路了
- 聯絡我們

竹北場導覽列項目如下：
- 場地資訊
- 時間表
- 節目表
- 聯絡我們

請以手機版優先設計，導覽列可以是：
- 上方 sticky tab / sticky nav
不要做側邊導覽，因為手機操作不方便。

## 每個活動資訊頁的內容區塊

### 1. 頁面頂部資訊
請在活動資訊頁最上方顯示：
- 場次名稱（例如：斗南場）
- 簡短說明文字
- 一個醒目的返回首頁按鈕或連結

### 2. 場地資訊
斗南場與竹北場的場地差異很大，場地資訊請分開成兩套 code / 兩套區塊各自陳列，不要共用同一個 generic venue component 硬套資料。

建議實作方向：
- 斗南場使用 `DounanVenueSection`
- 竹北場使用 `ZhubeiVenueSection`
- 兩者可共用小型 UI 元件，例如 icon、按鈕、圖片容器，但主要內容結構、欄位順序與呈現方式各自維護
- 場地資訊資料也分開管理，避免用同一份 `venueFacilities` 塞入大量 optional 欄位

此區塊要有：
- 會場引導圖片區
- Google Maps 座標轉跳按鈕
- Apple Maps 座標轉跳按鈕（若需要）
- 簡短文字說明，協助使用者抵達園區後找到指定會場

已有素材的區塊請優先使用現有素材；尚未確認或尚未補齊的圖片才使用明顯 placeholder，並標示「圖片待補」。現有素材可作為第一版使用，日後仍可替換，不視為最終定稿。

#### 斗南場場地資訊

斗南場場地資訊第一版只需要幫助使用者找到會場，不需要細分報到區、急救站、補水區、廁所、服務台等站點。這些內容若後續要補，先放在場地引導圖片或備用圖中，不列入第一版主要資料欄位。

斗南場目前已知主要資料：
- 會場：虎尾建國眷村 已棟蓄水池周邊草地 C 區
- Google Maps 座標：`23°42'49.7"N 120°25'17.8"E`
- 十進位座標：`23.713806, 120.421611`
- 場地圖片素材：
  - `source-materials/dounan/venue/dounan-zone-map.jpg`
  - `source-materials/dounan/venue/dounan-map-screenshot.png`
  - `source-materials/dounan/venue/dounan-venue-photo.png`

前端實作時，請將需要使用的圖片複製到 `public/assets` 或等效的公開資產資料夾，並使用清楚的英文檔名。不要直接從 `source-materials/...` 作為正式前端引用路徑。

#### 竹北場場地資訊

竹北場場地在商辦中，場地資訊請做成一頁式的文字配圖抵達流程，不需要做得太複雜，也不要沿用斗南場的戶外園區導航結構。

竹北場使用 `ZhubeiVenueSection` 獨立實作，請與斗南場的 `DounanVenueSection` 切分開來。兩者可共用小型 UI 元件，但竹北場主要內容結構、欄位順序與呈現方式應獨立維護。

竹北場場地資訊第一版需要呈現：
- Google Maps 圖或地圖截圖 placeholder
- 商辦 / 商場地址與座標 placeholder
- 商場內換證流程
- 搭電梯到指定樓層的指引
- 出電梯後找到會場的文字與圖片指引

竹北場場地資訊呈現方式：
- 以步驟式文字配圖為主
- 每一步可有一張圖片或 placeholder
- 圖片未補齊時顯示「圖片待補」
- 地址、座標、樓層、換證細節未確認時顯示「待補」
- 不要自行編造商辦地址、座標、樓層或換證規則

### 3. 時間表
場地資訊與時間表是不同區塊，不可互相取代。時間表圖片不可用來取代場地資訊；場地資訊需依前述 `DounanVenueSection` / `ZhubeiVenueSection` 規格獨立實作。

此區塊放公開給家長、學生與一般參加者看的活動時間表。工作人員與老師的內部流程不呈現在網站中，內部流程會另外在工作 LINE 群發布。

時間表需依場次獨立，不共用同一份資料：
- 斗南場使用 `DounanScheduleSection`
- 竹北場使用 `ZhubeiScheduleSection`
- 時間表資料請使用 `data/schedules.public.json`

時間表圖檔本身已經是對外版且資訊明確，網站請直接呈現時間表圖檔，不要另外拆資料重做 timeline 或卡片。請保留圖片原本的溫暖海報風格。

時間表圖片呈現要求：
- 手機版寬度以容器滿版呈現
- 圖片需完整顯示，不要裁切關鍵內容
- 點擊圖片後需以 lightbox / modal 方式放大檢視
- 放大檢視時圖片仍需完整顯示，並提供明顯的關閉按鈕
- 圖片周邊可放簡短 caption 或說明文字，但不要建立另一份平行文字版時間表

請不要呈現以下內部工作資訊：
- 樂器裝車
- 樂器搬運
- 場地布置
- 音控定位或設備確認
- 老師集合或行前會議
- 工作人員就定位
- 收尾作業
- 物品上車

若圖片後續更新，請替換圖片檔或新增新版圖片，不要在網站另建一份平行時間表資料。

### 4. 節目表
此區塊放節目資訊，需使用「折疊列表 accordion」呈現，避免把多張節目表圖片直接堆在頁面中造成閱讀混亂。

#### 斗南場節目表結構

斗南場節目表較複雜，請依照以下層級呈現：

1. 場次：上午場 / 下午場
2. 老師與時間：作為折疊列表標題
3. 學生節目明細：作為折疊展開內容

折疊列表預設全部收起，使用者點擊「老師名稱 + 時間」後展開該老師底下的學生節目明細。

斗南場折疊列表標題請參考「老師場次分配表」總覽圖，格式建議如下：
- Miss. Candy｜10:00 - 10:20
- Mr. Vincent｜10:20 - 10:50
- Miss. Khristin｜10:50 - 11:10
- Miss. Wanda｜11:10 - 11:30
- Miss. Lucy｜11:30 - 12:25
- Miss. Linda｜12:25 - 12:50
- Miss. Vivian｜12:50 - 13:05
- Orange 呂老師｜13:30 - 13:40
- Mr. Stone 石老師｜13:40 - 14:10
- Mr. Ben 廖老師｜14:10 - 14:25
- Mr. Nick 張老師｜14:25 - 14:50
- Mr. Kevin 張老師｜14:50 - 15:40
- Mr. Yang 李老師｜15:40 - 16:05

斗南場節目資料已整理在 `data/programs.dounan.json`，前端節目表、accordion 與搜尋結果都應從此 JSON 讀取或衍生，不另外維護第二份節目資料。資料欄位至少包含：
- 序號
- 學生姓名
- 曲目
- 備註（若原圖有標示）

若圖片中有樂器、合奏、伴奏或其他欄位，也請保留成可選欄位，避免後續資料遺失。

#### 竹北場節目表結構

竹北場節目表不需要轉成斗南場的 accordion / JSON 結構，直接在節目表區塊呈現單張節目表圖片即可。竹北場節目表區塊需預留少量文字說明空間，方便補充演出順序、注意事項或圖片閱讀提醒。

竹北場節目表素材位置：
- 資料夾：`source-materials/zhubei/programs`
- 圖片：`zhubei-program-sheet-01.png`

竹北場圖片呈現要求：
- 圖片需完整顯示，不要裁切關鍵內容
- 手機版寬度以容器滿版呈現，保留可讀性
- 點擊圖片後需以 lightbox / modal 方式放大檢視
- 放大檢視時圖片仍需完整顯示，並提供明顯的關閉按鈕
- 放大檢視需支援手機操作，避免圖片被固定容器裁切
- 圖片需提供替代文字，例如「竹北場節目表」
- 圖片周邊需預留文字說明區，可先顯示「節目表說明待補」
- 竹北場不需要節目搜尋欄位
- 竹北場不需要從 `data/programs.dounan.json` 或其他 `programs` JSON 讀取節目資料

#### 節目表 UI 要求

以下要求適用於斗南場節目表：

- 手機版優先，折疊標題要大且好點擊
- 預設收起，避免頁面一打開就過長
- 每個折疊標題需清楚顯示老師與時間
- 展開內容以清楚列表呈現，不要直接嵌入節目表圖片
- 節目表上方必須提供搜尋欄位，讓使用者搜尋老師、學生或曲目
- 若某位老師底下暫時沒有節目明細資料，展開內容請顯示「節目明細待補」

#### 節目表搜尋互動

此搜尋互動只適用於斗南場。竹北場不提供節目搜尋。

搜尋欄以 `mockups/program-option-1-accordion-cards.svg` 的卡片式 accordion 為基底設計。

搜尋行為：
- 使用者輸入姓名或關鍵字時，不需要每打一個字就即時過濾
- 每次輸入後，當搜尋欄位失焦（unfocus / blur）時才檢查目前輸入值
- 搜尋範圍包含老師名稱、學生姓名、曲目名稱
- 搜尋可跨上午場 / 下午場與所有老師資料
- 搜尋文字清空後，恢復預設的全資料顯示狀態

搜尋中與搜尋結果狀態：
- 有搜尋文字且找到結果時，節目表區塊顯示「符合搜尋項目的節目」
- 搜尋結果以老師為群組呈現
- 老師群組標題顯示老師名稱，必要時可補充該老師時段
- 結果項目的 UI 元件不變，最小單位仍是原本節目卡片：演出順序、演出者名稱、曲目
- 搜尋結果不顯示完整 accordion 全資料，只顯示符合搜尋條件的節目卡片

搜尋結果範例：

搜尋 `王錦聰`，若只在 Miss. Lucy 張老師底下有兩筆結果，呈現：
- Miss. Lucy 張老師
  - 03｜王錦聰｜My heart will go on.
  - 06｜王錦聰｜卡農

搜尋 `吳映潔`，若在 Miss. Candy 蔡老師與 Mr. Ben 廖老師底下各有一筆結果，呈現：
- Miss. Candy 蔡老師
  - 03｜吳映潔｜再見的時候
- Mr. Ben 廖老師
  - 01｜吳映潔｜曲目待補

空結果狀態：
- 若搜尋不到任何資料，顯示空畫面
- 空畫面文字顯示：「查無結果」
- 可補一行輔助文字：「請確認姓名或曲目是否輸入正確」

### 5. 我迷路了
此區塊只適用於斗南場。竹北場不需要「我迷路了」功能，也不顯示此區塊或對應導覽 tab。

斗南場的「我迷路了」區塊非常重要，請做成醒目區塊。
放兩個大按鈕：
- 用 Google 地圖拯救我
- 用 Apple 地圖拯救我

斗南場目前已有座標與地圖連結，請使用斗南場資料設定目的地。

### 6. 聯絡我們
請顯示：
- LINE 官方帳號
- Facebook 粉絲專頁
- LINE ID

先用 placeholder 資訊即可，例如：
- LINE 官方帳號：待補
- Facebook：待補
- LINE ID：待補

並保留可點擊連結的結構。

## 技術與輸出需求
請直接產出一個可執行、可預覽的前端頁面版本。
需求如下：

- 使用 Vite + React 實作
- 不使用純 HTML / CSS / JS 作為第一版主實作方式
- 結構要清楚，方便我之後自行替換內容
- 各區塊請加上清楚註解，標示哪些內容是 placeholder、哪些之後要替換
- 所有版面請以手機優先設計，同時兼容桌機
- 導覽列需支援平滑捲動到指定區塊
- 視覺風格乾淨、活動感、但不要太商業或太花俏
- 不需要後端
- 不需要資料庫
- 不需要做登入功能

## 內容處理原則
因為我目前還沒有完整資料，請你遵守以下原則：
- 不要自行編造過多真實細節
- 可以用少量假資料示意版面
- 已有圖片素材的區塊請優先使用現有素材，其餘未確認圖片才用 placeholder 區塊呈現
- 所有待補內容請清楚標示，例如：
  - 圖片待補
  - 活動時間表待補
  - 節目明細待補
  - Google Maps 連結待補
  - Apple Maps 連結待補
  - LINE / Facebook / LINE ID 待補

## 檔案與程式碼要求
請提供完整程式碼，並讓我能快速看懂結構。
如果是多個頁面，請幫我規劃清楚的檔案結構。
請使用 Vite + React 的專案結構，並將元件拆分合理，例如：
- HomePage
- EventPage
- SectionNav
- VenueSection
- DounanScheduleSection
- ZhubeiScheduleSection
- ProgramSection
- ProgramAccordion
- DounanLostSection
- ContactSection

## 命名
請使用清楚易懂的英文檔名與變數命名，但頁面呈現文字請用繁體中文。

## 最後請額外做到
1. 讓首頁兩個大按鈕在手機上非常醒目
2. 讓活動資訊頁的 sticky 導覽列在手機上好操作
3. 讓斗南場「我迷路了」區塊特別明顯；竹北場不顯示「我迷路了」
4. 讓整體看起來像可直接上線的框架版，而不是很隨便的 wireframe
