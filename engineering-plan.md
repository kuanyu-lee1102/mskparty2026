# 活動網站工程架構規劃

更新日期：2026-05-04  
專案目標：先完成可上線的活動網站框架，支援手機優先瀏覽，提供首頁分流與兩個場次的一頁式活動資訊頁。

## 一、目前需求摘要

依據 [event-website-spec.md](/Users/kuan-yu/Documents/2026%20音樂會/event-website-spec.md) 的內容，網站目前定位如下：

- 純前端網站
- 不需要後端
- 不需要資料庫
- 不需要登入功能
- 首頁作為場次分流入口
- 各場次頁面為一頁式資訊頁
- 以手機版操作體驗為優先
- 目前多數內容可先以 placeholder 或假資料呈現
- 斗南場節目表需支援「場次 > 老師與時間 > 學生節目明細」的折疊列表結構
- 斗南場場地資訊第一版以「會場引導圖片」與「Google Maps 座標轉跳」為核心，不細分報到區、急救站、補水區等站點

## 二、已確認的工程方向

目前已經確認的工程決策：

- 技術方案：使用 `Vite + React`，不採純 HTML / CSS / JS 作為主實作方式
- 路由方式：使用正常路由，不使用 hash route
- 頁面路由：
  - `/`：首頁
  - `/dounan`：斗南場
  - `/zhubei`：竹北場
- 場次命名：
  - 使用者介面顯示「斗南場」與「竹北場」，主標題與首頁按鈕不包含日期
  - 程式、資料、元件命名使用 `dounan` 與 `zhubei`
  - 日期若需要顯示，放在活動資訊補充文字或時間資訊中，不放進主要場次名稱與首頁按鈕
- 部署目標以 Cloudflare Pages 為主，需處理 SPA fallback，避免直接開啟 `/dounan` 或 `/zhubei` 時出現 404

仍需後續確認的主題：

- GitHub repo
- domain
- service
- publish

這些方向是正確的，但若要讓網站後續能穩定維護，還需要補上更多規劃項目。

## 三、除了 repo / domain / service / publish，還需要規劃的項目

### 1. 內容維護方式

需要先決定未來真實資料要如何更新，避免每次都直接改版面程式碼。

建議：

- 將兩個場次資料拆成獨立資料檔
- 版型與資料分離
- 未來只修改資料內容，不動主要頁面結構

可管理的內容包含：

- 場次名稱
- 場地引導圖
- 周邊設施資訊
- 活動時間表
- 節目表
- Google Maps / Apple Maps 連結
- 聯絡資訊

節目表資料需與 UI 分離。斗南場因為資料層級較複雜，建議資料結構保留：

- 場次名稱：上午場 / 下午場
- 場次開始時間
- 老師名稱
- 老師演出時段
- 學生節目明細

前端 UI 以 accordion 呈現，預設收起。折疊標題顯示「老師名稱 + 時間」，展開後顯示學生節目明細。OCR 圖片轉文字完成前，展開內容可先顯示「節目明細待補」。

斗南場目前唯一節目資料來源為 `data/programs.dounan.json`。前端節目表、accordion 與搜尋結果都應從此 JSON 讀取或衍生，不另外維護 CSV、報名表或圖片轉文字副本。節目表圖片與正式報名表可作為人工校對來源，但不作為前端資料來源。

竹北場節目表不納入 `programs` JSON 資料化流程，前端直接呈現單張節目表圖片：

- 資料夾：`source-materials/zhubei/programs`
- 圖片：`zhubei-program-sheet-01.png`

竹北場不需要節目搜尋欄位，也不需要 accordion 展開結構。

斗南場節目表區塊必須支援搜尋欄位。搜尋資料不應另外維護一份，而是從同一份 `programs` 資料衍生。搜尋邏輯需要支援：

- 搜尋老師名稱
- 搜尋學生姓名
- 搜尋曲目名稱
- 跨上午場 / 下午場搜尋
- 搜尋結果依老師分組
- 搜尋結果卡片沿用節目明細的最小 UI 單位：演出順序、演出者名稱、曲目
- 搜尋欄失焦（blur / unfocus）時才執行搜尋
- 搜尋文字清空後恢復 accordion 全資料顯示
- 搜尋無結果時顯示「查無結果」空狀態

### 2. URL 與頁面策略

建議使用清楚、可分享、可做 QR code 的網址結構：

- `/`：首頁
- `/dounan`：斗南場
- `/zhubei`：竹北場

這樣有幾個好處：

- 使用者容易理解
- 工作人員容易口頭引導
- 現場輸出物可直接放 QR code
- 後續若要新增場次也容易擴充

### 3. 檔案與資產管理

第一版圖片素材策略已確認：**已有素材直接使用，其他未確認內容才使用 placeholder**。現有素材可作為第一版預覽與實作使用，日後仍可替換，不視為最終定稿。

第一版可使用的現有素材：

- 斗南場場地資訊：
  - `source-materials/dounan/venue/dounan-zone-map.jpg`
  - `source-materials/dounan/venue/dounan-map-screenshot.png`
  - `source-materials/dounan/venue/dounan-venue-photo.png`
- 竹北場節目表：
  - `source-materials/zhubei/programs/zhubei-program-sheet-01.png`

前端實作時，請將需要使用的圖片複製到 `public/assets` 或等效的公開資產資料夾，並使用清楚的英文檔名。不要直接從 `source-materials/...` 作為正式前端引用路徑。

雖然目前已有部分素材，但之後需要被替換或補齊的內容仍需列清單。

建議建立上線前待補項目清單：

- 活動時間表
- 節目表 OCR 文字資料
- 老師場次與節目明細對應
- Google Maps 正式連結
- Apple Maps 正式連結
- LINE 官方帳號
- Facebook 粉絲頁
- LINE ID

### 4. 發布流程與權限

需要先定誰能更新內容、誰能發布，以免活動前臨時修改時流程混亂。

建議先定義：

- 誰負責改內容
- 誰負責確認內容正確
- 誰負責正式發布
- 若活動前一天需要修正，流程怎麼走

### 5. 手機優先 QA

這個網站明顯是活動參與者在手機上即時查看資訊，所以測試重點不只是畫面好看，而是現場是否順手。

建議確認：

- iPhone 與 Android 的瀏覽體驗
- 4G / 5G 環境下的載入速度
- sticky nav 是否遮擋內容
- 導覽列是否易點擊
- 地圖按鈕是否可正常開啟對應地圖
- 字級在戶外環境是否夠清楚

### 6. 監測與備援

即使是小型活動網站，也建議預先規劃最基本的營運備援。

至少可考慮：

- 基本流量分析
- 404 頁面
- 平台預設網址作為備援入口
- 若地圖連結異常時的文字備註或地址顯示

## 四、是否能以免費服務達成

結論：**可以，100 人規模非常適合用免費方案處理。**

理由如下：

- 目前需求是純靜態前端網站
- 沒有會員系統
- 沒有後端 API
- 沒有資料庫讀寫
- 沒有高頻互動或即時功能

在這種前提下，100 人左右的活動參與者使用量非常輕，通常不需要付費雲端架構。

## 五、建議的部署方式

### 首選：GitHub + Cloudflare Pages + 自購網域

建議原因：

- 適合純靜態網站
- 免費方案已足夠支撐此專案
- 支援自動部署
- 支援 HTTPS
- 支援自訂網域
- 分享與維護流程都相對簡單

建議流程：

1. 使用 GitHub 建立 repository
2. 將網站程式碼放入 repo
3. 連接 Cloudflare Pages
4. 每次 push 到指定 branch 時自動部署
5. 綁定正式網域

### 次選：GitHub Pages + 自購網域

若希望流程更單純，也可直接使用 GitHub Pages。

優點：

- 設定簡單
- 免費
- 適合純靜態網站

相較之下，Cloudflare Pages 在部署體驗、CDN、彈性與後續擴充上通常更合適。

## 六、為什麼目前不需要複雜服務

現階段不建議一開始就規劃：

- 後端伺服器
- 資料庫
- CMS
- 帳號系統
- 複雜 API 架構

原因是：

- 專案需求目前很明確偏靜態展示
- 使用者人數不高
- 上線時程與維護成本應盡量簡化
- 複雜架構只會增加風險與修改成本

## 七、建議的初始工程架構

### Vite + React（已決定採用）

適合條件：

- 希望元件化
- 希望後續更容易擴充頁面區塊
- 希望資料與 UI 更清楚分離

目前已決定採用 **Vite + React**。  
原因是此專案包含兩個場次頁、斗南場節目表 accordion、搜尋互動、資料 JSON 與後續內容替換需求，使用 React 較容易將資料、頁面、元件與互動邏輯分離。

### 建議的資料檔方向

建議至少拆出以下資料檔或資料模組：

- `events`：兩個場次基本資料
- `dounanVenue`：斗南場場地資訊，包含會場名稱、C 區位置描述、Google Maps 座標、地圖連結與場地引導圖片
- `zhubeiVenue`：竹北場場地資訊，依竹北場實際場地條件獨立整理，不沿用斗南場欄位
- `dailySchedules`：整日活動流程
- `programs`：節目表資料，支援斗南場的上午/下午與老師折疊層級
- `programSearch` 或同等 helper：由 `programs` 衍生搜尋結果，依老師群組回傳符合項目
- `contacts`：LINE、Facebook、LINE ID 等聯絡資訊

場地資訊不建議做成單一共用 `VenueSection` 後用資料切換，因為斗南場與竹北場場地差異大，若硬共用會產生太多 optional 欄位與條件判斷。建議拆成：

- `DounanVenueSection`
- `ZhubeiVenueSection`

兩個場地區塊可以共用底層 UI 小元件，例如 `InfoCard`、`MapButton`、`AssetImage`，但主要 code 各自陳列，讓未來修改斗南場動線時不影響竹北場，修改竹北場場地配置時也不需要理解斗南場邏輯。

斗南場節目資料已先整理為 `data/programs.dounan.json`。若後續需要修正節目內容，應直接更新此 JSON，並讓任何匯出、試算表或檢查報告都從此 JSON 重新產生，避免多份資料來源互相漂移。

純 HTML / CSS / JS 不作為第一版主實作方案。

## 八、目前最建議的組合

若以這個專案目前規模來看，建議優先採用：

- `GitHub repository`
- `Vite + React`
- `Cloudflare Pages`
- `自購網域`
- `main branch 自動部署`

## 九、後續建議補上的規劃文件

接下來可以再補成獨立文件的內容：

1. repo 結構規劃
2. 頁面路由規劃
3. 資料檔格式規劃
4. 節目表 OCR 轉文字後的資料匯入規則
5. 斗南場與竹北場各自的場地資訊更新規則
6. 部署流程
7. 上線前 checklist
8. 活動前一天的內容確認流程

## 十、目前結論

### 工程層面

此專案可視為小型靜態活動網站，重點在：

- 手機版資訊呈現
- 內容可快速替換
- 網址清楚可分享
- 發布流程簡單穩定

### 成本層面

若參與人數約 100 人，**使用免費服務完全可行**，不需要一開始就投入付費主機或複雜後端。

### 推薦方案

目前最推薦：

- GitHub 管理程式碼
- Cloudflare Pages 部署
- 綁定正式 domain
- 保持純靜態網站架構

---

## 後續可延伸

下一步可直接整理：

- `repo / 資料夾結構`
- `頁面與元件切分`
- `資料檔 schema`
- `部署與發布 SOP`
